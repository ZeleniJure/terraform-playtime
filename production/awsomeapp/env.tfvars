# maybe some static VPC definitions, ...
# env_name = "prod"

# usually these are used locally. But can also be nice for CI to just get values from here!
# An example would be to put such a file into production folder and use it to get environment specifics automatically
